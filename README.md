# This is a section
# git status to see if there is any changes to files or folders within the respository
# git add folder or file name (if we specify the name only the specified files or folders will be staged)
# git add . means to stage all files that were changed
# git add * means to stage everything
# git push origin main means push to main branch
# git push origin branch_A means to push to branch_A 